# Production environment variables template for Next.js app
# Copy this file to ".env" (for local) or set these in your hosting provider (Vercel/Render).
# Do NOT commit secrets. Keep .env out of version control.

# General
# Set to "production" for production deployments
NODE_ENV=production

# Build/runtime toggles
# If set to "1" or "true", env validation is skipped. Keep "false" in production.
SKIP_ENV_VALIDATION=false

# =========================
# Server-side required vars
# =========================
# Firebase Admin (used on server only)
FIREBASE_ADMIN_PROJECT_ID=
FIREBASE_ADMIN_CLIENT_EMAIL=
# IMPORTANT: If your private key contains newlines, keep it quoted and use \n for newlines.
# Example: "-----BEGIN PRIVATE KEY-----\nABC...\n-----END PRIVATE KEY-----\n"
FIREBASE_ADMIN_PRIVATE_KEY=""

# Razorpay (server-side secret)
RAZORPAY_KEY_ID=
RAZORDAY_KEY_SECRET=

# Optional server-side (email + cloudinary)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM_EMAIL=

CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# =========================
# Client-side required vars
# (must start with NEXT_PUBLIC_)
# =========================
NEXT_PUBLIC_RAZORPAY_KEY_ID=
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

# Optional client-side
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=
# Toggle membership features on client; defaults to "true" if unset
NEXT_PUBLIC_MEMBERSHIP_ENABLED=true

# =========================
# Notes for Hosting
# =========================
# Vercel:
# - Add all of the above in Project Settings -> Environment Variables.
# - Use "Production" environment for production deployment.
# - vercel.json already sets "installCommand": "npm install" and "buildCommand": "npm run build".
#
# Render:
# - Create a "Web Service" (Node) with Build Command "npm install && npm run build"
#   and Start Command "npm run start".
# - Add all environment variables under "Environment".
# - Set Node version in "Environment" or via package.json "engines".
